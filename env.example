# API Configuration
# For development: use proxy (VITE_API_BASE_URL=/api)
# For production: use full URL (VITE_API_BASE_URL=https://your-api-domain.com/api)
VITE_API_BASE_URL=/api

# App Configuration
VITE_APP_NAME=DevCommunity
VITE_APP_VERSION=1.0.0

# Environment
NODE_ENV=development

# Optional: Analytics (if you plan to add them)
# VITE_GOOGLE_ANALYTICS_ID=your_ga_id
# VITE_SENTRY_DSN=your_sentry_dsn

# Backend Configuration (for reference)
# BACKEND_PORT=8000
# BACKEND_HOST=localhost
